# Test-only profile for the zynera quota-enforcement lab scenario
# (../zynera/scripts/test-lab-quota-enforcement.sh) — not real calibration
# data, just enough to let Zyvor Janus simulate the exported job.
model: quota-test-a
profiles:
  A100:
    runtime_seconds: 60
    gpu_memory_gb: 1
