# Test-only profile for the zynera MIG lab scenario
# (../zynera/scripts/test-lab-mig-scheduling.sh) — not real calibration
# data, just enough to let Zyvor Janus simulate the exported job.
model: mig-dispatch-test
profiles:
  H100:
    runtime_seconds: 60
    gpu_memory_gb: 1
  any:
    runtime_seconds: 60
    gpu_memory_gb: 1
